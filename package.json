{
  "name": "tang-npm-monorepo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "demo": "pnpm --filter demo dev",
    "start:cli": "pnpm --filter @tanggoat/pack-starter start",
    "registry": "pnpm --filter @tanggoat/npm-helper registry",
    "release": "./scripts/release.sh --stable",
    "release:beta": "./scripts/release.sh --beta",
    "release:alpha": "./scripts/release.sh --alpha",
    "release:rc": "./scripts/release.sh --rc",
    "release:dry-run": "./scripts/release.sh --stable --dry-run",
    "preinstall": "npx only-allow pnpm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.4.1"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@types/node": "^24.1.0",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=7.0.0"
  }
}
